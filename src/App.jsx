export default function App() {
  return (
    <main style={{ padding: 'var(--space-7)', minHeight: '100vh' }}>
      <h1 className="font-display" style={{ color: 'var(--gold)' }}>
        Deckora
      </h1>
      <p className="font-body-lg" style={{ marginTop: 'var(--space-4)' }}>
        Design tokens cargados correctamente.
      </p>
    </main>
  );
}
